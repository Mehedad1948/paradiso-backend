<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>You are invited to join a room</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        color: #333333;
        background-color: #f9f9f9;
        padding: 20px;
      }
      .container {
        max-width: 600px;
        margin: auto;
        background: #ffffff;
        padding: 30px;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
      }
      .button {
        display: inline-block;
        margin-top: 20px;
        padding: 10px 20px;
        background-color: #28a745;
        color: #ffffff;
        text-decoration: none;
        border-radius: 4px;
      }
      .footer {
        margin-top: 30px;
        font-size: 12px;
        color: #999999;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2>Hello,</h2>

      <p>
        <strong><%= inviterUsername %></strong> has invited you to join a room
        on our platform.
      </p>

      <p><strong>Your Email:</strong> <%= email %></p>

      <p>Click the button below to accept the invitation and join the room:</p>

      <a href="<%= invitationUrl %>" class="button">Join Room</a>

      <p>
        If you weren’t expecting this invitation, you can safely ignore this
        email.
      </p>

      <div class="footer">
        &copy; <%= new Date().getFullYear() %> Your App Name. All rights
        reserved.
      </div>
    </div>
  </body>
</html>
